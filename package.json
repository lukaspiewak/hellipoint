{
  "name": "heliopolis",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b",
    "pretest": "pnpm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "dev": "pnpm --filter @heliopolis/client dev"
  },
  "devDependencies": {
    "@heliopolis/render": "workspace:*",
    "@heliopolis/sim": "workspace:*",
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
